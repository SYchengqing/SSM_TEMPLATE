<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cq.mapper.AccountMapper">
    <update id="updateAccount">
        update scott.account set money = money + #{param2} where id = #{param1}
    </update>
</mapper>